pipeline {
    agent { label 'agent4' }

    stages {

        stage('Git Checkout') {
            steps {
                git branch: 'main/testing',
                    url: 'https://github.com/hamsapriya2610-dotcom/git26.git'
            }
        }

        stage('Maven Build') {
            steps {
                sh 'mvn clean install'
            }
        }
    }
}
